{
  "name": "greenai",
  "private": true,
  "scripts": {
    "verify:openspec-docs": "node --test scripts/parity-docs-anchor.test.mjs",
    "verify:backend": "npm run build --prefix backend && npm run test --prefix backend",
    "ship": "node scripts/ship.mjs"
  }
}
