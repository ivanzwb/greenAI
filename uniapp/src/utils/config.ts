/** API 根地址（与 `miniprogram/utils/api.js` 的 BASE_URL 对齐；生产改为 HTTPS） */
export const API_BASE_URL = "http://127.0.0.1:3000";

/** 本地持久化 JWT 的 storage key */
export const TOKEN_STORAGE_KEY = "greenai_token";
